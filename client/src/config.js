const config = {
  // Use environment variable for API URL, fallback to production URL
  API_URL: process.env.REACT_APP_API_URL || 'https://clockinout-backend.onrender.com'
};

export default config; 